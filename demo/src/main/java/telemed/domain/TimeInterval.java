package telemed.domain;

/**
 * Enum representing a time interval from present time.
<#if type == "code">

<#include "/data/author.txt">
</#if>
 */
public enum TimeInterval {
  LAST_DAY, LAST_WEEK, LAST_MONTH
}
